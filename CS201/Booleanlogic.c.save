#include <stdio.h>
int main(){
  int A = 0;
  int B = 0;
  int C = 0;
  int D = 0;
  int ans = 0; 
scanf("A: %d\n", A);
scanf("B: %d\n", B);
scanf("C: %d\n", C);
scanf("D: %d\n", D);
if(!B && C || A && D || C && !B || B)

print("Answer: %d\n", ans);
return 0;
}
